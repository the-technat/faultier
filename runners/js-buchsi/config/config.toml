concurrent = 2
check_interval = 3

[session_server]
  session_timeout = 1800

[[runners]]
  name = "js-buchsi-runner"
  url = "https://code.immerda.ch"
  token = "4ZkFn_5K5i8asPMGszPx"
  executor = "docker"
  limit = 2
  request_concurrency = 2
  [runners.custom_build_dir]
  [runners.cache]
    [runners.cache.s3]
    [runners.cache.gcs]
    [runners.cache.azure]
  [runners.docker]
    tls_verify = false
    image = "docker:stable"
    privileged = false
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    volumes = ["/var/run/docker.sock:/var/run/docker.sock", "/cache"]
    shm_size = 0
