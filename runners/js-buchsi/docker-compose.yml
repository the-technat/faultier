version: '3'

services:
  js-buchsi-runner:
    image: gitlab/gitlab-runner:latest
    container_name: js-buchsi-runner
    restart: always
    volumes:
      - /volumes/runners/js-buchsi/config/:/etc/gitlab-runner/
      - /var/run/docker.sock:/var/run/docker.sock
