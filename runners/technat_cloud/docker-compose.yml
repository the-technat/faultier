version: '3'

services:
  technat-cloud-runner:
    image: gitlab/gitlab-runner:latest
    container_name: technat-cloud-runner
    restart: always
    volumes:
      - ./config/:/etc/gitlab-runner/
      - /volumes/runners/technat_cloud/cache:/cache
      - /var/run/docker.sock:/var/run/docker.sock
